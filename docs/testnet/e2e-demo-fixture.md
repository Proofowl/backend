# e2e demo fixture

This file is a **demo artifact**. It exists so the `proofowl-backend`
end-to-end pipeline demo has a real, merged Pull Request to discover and
verify against. It is not a feature, a fix, or documentation of anything
real. See `docs/testnet/e2e-demo.md` for the full record.
